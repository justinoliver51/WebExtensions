var numItems = $('.basePosition').length;
//alert(numItems);
alert("End of script...");

// To search for text
/*
var regex = /abelski/;

if(regex.test(document.body.innerText))
{
	alert("Got here!");
}
*/
